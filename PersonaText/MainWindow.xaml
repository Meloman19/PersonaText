<Window Name="MW" x:Class="PersonaText.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PersonaText"
        mc:Ignorable="d"
        Title="Persona Text Editor" Height="440.755" Width="803.431">

    <Grid>
        <Grid.Background>
            <SolidColorBrush Color="{DynamicResource {x:Static SystemColors.ControlColorKey}}"/>
        </Grid.Background>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>

        <Menu Grid.Row="0" x:Name="mainmenu">
            <MenuItem Header="File">
                <MenuItem Header="Open Project" IsEnabled="False" Name="mm_openproject" Click="mi_openproject_Click" ></MenuItem>
                <MenuItem Header="Save Project" IsEnabled="False"  Name="mm_saveproject" Click="mi_saveproject_click" ><!--IsEnabled="{Binding openfile}"--></MenuItem>
                <Separator></Separator>
                <MenuItem Header="Import" Name="mi_import" Click="mi_import_click"></MenuItem>
            </MenuItem>
            <MenuItem Header="Tools">
                <MenuItem Header="Export text from..." Name="mm_tools_export" Click="mm_tools_export_Click"></MenuItem>
            </MenuItem>
        </Menu>

        <Grid Grid.Row="1" Name="menug" Width="{Binding ElementName=IC_MSG, Path=ActualWidth}" HorizontalAlignment="Left">
            <Grid.ColumnDefinitions>
                <ColumnDefinition></ColumnDefinition>
                <ColumnDefinition></ColumnDefinition>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" BorderBrush="Black" BorderThickness="0,1,1,1">
                <Menu  x:Name="menuold" Background="#FFF0F0F0">
                    <MenuItem Header="Open OLD FNT" Name="Old_FNT_Open" Click="FNold"></MenuItem>
                    <MenuItem Header="Set Char" Name="Old_Char_Set" Click="CharSet"></MenuItem>
                    <MenuItem IsEnabled="{Binding openfile}" Header="Save as Text" Name="Old_Save" Click="STold"></MenuItem>
                </Menu>
            </Border>
            <Border Grid.Column="2" BorderBrush="Black" BorderThickness="1,1,0,1">
                <Menu  x:Name="menunew">
                    <MenuItem Header="Open NEW FNT" Name="New_FNT_Open" Click="FNnew"></MenuItem>
                    <MenuItem Header="Set Char" Name="New_Char_Set" Click="CharSet"></MenuItem>
                </Menu>
            </Border>
        </Grid>

        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">

            <ItemsControl Name="IC_MSG" ItemsSource="{Binding msg}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel>
                            <Label  Content="{Binding Path=Name}" HorizontalContentAlignment="Center"></Label>

                            <ItemsControl ItemsSource="{Binding Path=Strings}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition></ColumnDefinition>
                                                <ColumnDefinition></ColumnDefinition>
                                            </Grid.ColumnDefinitions>

                                            <TextBox Margin="2" Grid.Column="0" TextWrapping="Wrap" IsReadOnly="True" Text="{Binding Path=mystr_old, Mode=OneWay}" ></TextBox>
                                            <TextBox AcceptsReturn="True" Margin="2" Grid.Column="1"  TextWrapping="Wrap" Text="{Binding Path=mystr_new, Mode=TwoWay}" ></TextBox>
                                        </Grid>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>

                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

        </ScrollViewer>

    </Grid>

</Window>
